export default class User {
    constructor(firstName, lastName, name, email, password) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.name = name;
        this.email = email;
        this.password = password;
        
    }
}